import styled from 'styled-components';

export const StyledDashboard = styled.a`

    .cont {
        justify: left;
        text-align: left;
        align: left;
    }

    .stickyHeader {
        background: #087096;
        font-size: 3rem;
        height: 10vh;
        color: white;
        position: sticky;
    }

    .dataTable {
        background: #b9c2c6;
        padding: 1vh;
        width: auto;
    }
`