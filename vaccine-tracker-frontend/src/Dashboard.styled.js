import styled from 'styled-components';

export const StyledDashboard = styled.a`


    .stickyHeader {
        background: #087096;
        font-size: 2.6rem;
        height: 10vh;
        color: white;
        position: sticky;
        padding-top: .5vh;
    }

    .pageTitle {
        font-size: 1.5rem;
        padding-top: 2.5vh;
        padding-bottom: 2vh;
    }

    .tableName {
        font-size: 1.15rem;
        padding-top: 1vh;
        padding-bottom: 2vh;
    }

    .dataTable {
        padding: 1vh;
        width: auto;
    }

    .lineGraph {
        padding: 0;
    }
`